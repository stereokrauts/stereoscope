/**
 * A definition of the namespaces (or modules)
 * used in this application.
 * 
 * A namespace is an empty object shell.
 * If the namespace object doesn't exists,
 * create an empty one.
 */

window.sk = window.sk || {};
sk.stsc = sk.stsc || {};
sk.stsc.model = sk.stsc.model || {};
sk.stsc.view = sk.stsc.view || {};
sk.stsc.collection = sk.stsc.collection || {};
sk.stsc.resources = sk.stsc.resources || {};
