/**
 * The router module is the center of the app.
 * Nearly everything that's clickable/touchable
 * will be processed by this class. 
 */
'use strict';
define([
        'jquery',
        'underscore',
        'backbone'
], function($, _, Backbone) {

	var Router = Backbone.Router.extend({
		
		routes: {
			
			
		}
		
	});
	
	return Router;
});