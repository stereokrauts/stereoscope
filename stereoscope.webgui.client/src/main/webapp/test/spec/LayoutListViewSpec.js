define(['underscore', 'backbone', 'view.layoutManager/LOManagerListView'],
		function(_, Backbone, LOManagerListView) {

	xdescribe('A Layout List View', function() {
		var dispatcher, layoutList;
		var oscListRequest = '/stereoscope/system/request/frontend/layoutList';
		var oscLayoutRequest = '/stereoscope/system/request/frontend/layout';
		var oscListResponse = '/stereoscope/system/response/frontend/layoutList';
		var oscLayoutResponse = '/stereoscope/system/response/frontend/layout';

		beforeEach(function() {

		});

		afterEach(function() {

		});

		it('should be able to create its test objects', function() {

		});

		it('should fail if not properly initialized', function() {

		});

		it('should be bound to a dom element', function() {

		});

	});
});